[tool.poetry]
name = "course-6-web"
version = "0.1.0"
description = ""
authors = ["ivan"]
readme = "README.md"
packages = [{include = "course_6_web"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
